name: daily report on test branch

on:
  schedule:
    - cron: '*/2 * * * *'  # Run every two minutes
  push:
    branches:
      - test  # Change this to your default branch name if needed

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "hello nandkumar Babar this 4AM now"
